# Development environment template.
# Prefer generating .env with: python3 scripts/bootstrap_env.py
# Never commit the generated .env file.
APP_ENV=development
POSTGRES_DB=autorelay
POSTGRES_USER=autorelay
POSTGRES_PASSWORD=replace-with-a-long-random-development-password
# This host is correct for locally running API/worker processes. Docker Compose
# overrides DATABASE_URL so its containers connect to the `postgres` service.
DATABASE_URL=postgresql+asyncpg://autorelay:replace-with-the-same-password@localhost:5432/autorelay
FERNET_KEY=replace-with-a-valid-fernet-key
SESSION_COOKIE_SECURE=false
SESSION_TTL_HOURS=168
CORS_ORIGINS=http://localhost:5173,http://localhost:8080
PUBLIC_BASE_URL=http://localhost:8080
ALLOW_PRIVATE_ACTION_TARGETS=false
WEBHOOK_MAX_PAYLOAD_BYTES=65536
ACTION_TIMEOUT_SECONDS=10
WORKER_POLL_INTERVAL_SECONDS=1
WORKER_CONCURRENCY=4
WORKER_MAX_ATTEMPTS=3
WORKER_STALE_AFTER_SECONDS=300
WORKER_RETRY_BASE_SECONDS=2
WORKER_RETRY_MAX_SECONDS=60
LOG_LEVEL=INFO
